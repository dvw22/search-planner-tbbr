function [unsearched_area,unoccupied_cells] = unoccupied_area(occupancy_matrix, map)
%unsearched_area Calculates the area in the map that has still been
%unsearched
%   search_matrix is an array populated only by 0s and 1s. Units are in metres

binary_occupancy_matrix = occupancy_matrix>map.ProbabilitySaturation(1);

num_occupied_cells = 

unoccupied_cells = numel(occupancy_matrix) - sum(sum(search_matrix));      % calculate number of unsearched cells
unsearched_area = (1/map.Resolution)^2 * unsearched_cells;    % find total unsearched area

end

