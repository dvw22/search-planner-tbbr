function [map_waypoints,region_idx] = map_search_path(inputArg1,inputArg2)
% map_search_path Outputs a full waypoint list with a region indices matrix
% to access segmented regions during the waypoint planning process.
%   The segments are either a cell regi
outputArg1 = inputArg1;
outputArg2 = inputArg2;
end

